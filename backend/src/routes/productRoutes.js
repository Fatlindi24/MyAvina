// // import express from 'express';
// import {
//   getAllProducts,
//   getProductBySlug,
// } from '../controllers/productController.js';

// // Create a new router instance from Express
// const router = express.Router();

// // Define the route for getting all products
// // When a GET request is made to '/', it will call the getAllProducts function
// router.route('/').get(getAllProducts);

// // Define the route for getting a single product by its slug
// // When a GET request is made to '/:slug', it will call the getProductBySlug function
// router.route('/:slug').get(getProductBySlug);

// // Export the router so it can be used in your main server file
// export default router;